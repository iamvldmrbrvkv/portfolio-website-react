export interface Project {
  title: string;
  description: string;
  link: string;
  github: string;
}

export interface Skill {
  name: string;
  icon: string;
  alt: string;
}

export interface ContactLink {
  name: string;
  url: string;
  icon: string;
  alt: string;
}